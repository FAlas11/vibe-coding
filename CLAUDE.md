# CLAUDE.md — 平面记账

> 这个文件是本项目的**最高工作准则**。Claude Code 在本项目中的每一次操作都必须遵守本文件。
> 最后更新：2026-09-17

---

## 🚨 第一准则：技术决策的沟通方式（最高优先级，不可违反）

### 背景

本项目的用户**不是程序员**，这是他们第一次使用 Claude Code。用户明确要求：

> "在整个项目开发中，任何关于技术方面的事情，我是小白，我无法提供具体的要求，必须是你列出方案给我，并向我解释各个方案，由我来决定。"

### 因此，以下规则具有最高优先级，覆盖 Claude 的默认行为：

#### 规则 1：绝不替用户做技术决定

遇到任何**技术选择**时，**禁止**直接选定方案然后开始做。必须：

1. **列出 2–4 个候选方案**（不要只给一个，也不要给十个）
2. **用大白话解释每个方案**——杜绝"异步"、"依赖注入"、"服务端渲染"这类术语孤岛。必须用比喻或生活化的例子说明
3. **列出每个方案的优劣势**，尤其是**对用户的真实影响**（花不花钱、快不快、以后好不好改、数据安不安全），而不是技术指标
4. **给出明确推荐**，并说明"我为什么推荐它"、"我推荐的方案代价是什么"
5. **明确问用户，等用户回答**，然后才动手

#### 规则 2：什么算"技术决策"（必须问的）

- 用什么技术栈 / 框架 / 编程语言
- 用什么数据库 / 数据存哪里
- 要不要引入某个第三方库
- 项目文件怎么组织（当有多种合理组织方式时）
- 部署到哪里、怎么部署
- 要不要写自动化测试、写到什么程度
- 任何**会让用户花钱**的决定（服务器、域名、账号）
- 任何**难以撤销**的决定（一旦做了以后很难改的）
- 任何会改变用户**使用体验**的决定（多一步点击、多一个页面）

#### 规则 3：什么不算"技术决策"（可以直接做的）

以下情况**不需要问**，直接做即可，做完了用一句话说明就行：

- 纯粹的 bug 修复（用户报告的问题，按用户期待的行为修好）
- 代码风格、命名、注释
- 实现细节（循环怎么写、函数怎么拆），不影响用户看到的东西
- 已经在本文件中明确记录过的决定（见第 2 节、第 4 节）
- 用户明确说"你看着办"的部分

#### 规则 4：每次决策都要记录

用户做出决定后，**必须立刻把决定写进本文件的「决策记录」表**（见第 4 节），包含：决定了什么、为什么这么决定、被否决的方案是什么。这样下次对话不会重复提问，也不会悄悄改掉用户的选择。

#### 规则 5：说人话

- 面向用户的所有输出**用中文**
- 不要假设用户看得懂代码。解释时先给结论，再给理由，最后才是技术细节
- 报错时，先说"**这对我使用有什么影响**"，再说技术原因
- 不要用"简单"、"显然"、"只是"这类让非技术用户感到被贬低的词
- 涉及用户数据安全/金钱的内容，必须**主动、明确**提醒风险，不能因为"怕吓到用户"而省略

---

## 1. 项目简介

**平面记账** —— 一个极简的个人支出记账 Web App。

- 一句话定位：花完钱的那一刻，用手机花 5 秒钟把它记下来
- 用户：中文用户，人民币记账
- 形态：手机优先的网页应用（PWA，可添加到主屏幕、可离线使用）
- 数据：**只存在用户自己的设备上**。无服务器、无账号、无登录、不上传
- 核心：记一笔支出（金额 + 两级分类 + 备注 + 日期）

**完整产品设计见 [产品设计文档.md](产品设计文档.md)** —— 改动任何产品行为之前，先读那份文档。

---

## 2. 已确定的产品决定（用户已拍板，不要重新讨论）

| # | 决定 | 说明 |
|---|---|---|
| P1 | **形态：网页版 App（PWA）** | 手机浏览器打开 + 添加到主屏幕。不做原生 App、不做小程序、不做桌面版 |
| P2 | **数据只存本地 + 手动备份** | 不做账号、不做云端同步、不做服务器。备份靠导出/导入 JSON 文件 |
| P3 | **v1 只记支出** | 不记收入。数据结构预留扩展空间，但 v1 不实现 |
| P4 | **v1 功能范围：记流水 + 分类** | 统计图表、月度预算、Excel 导出**都不在 v1 范围内**（已在产品文档 3.2 节明确排除） |
| P5 | **两级分类：12 大类 / 99 小类** | 完整分类表见产品文档第 4 节，**是产品的一部分，改动需先问用户** |
| P6 | **所有项目文件直接放在项目根目录** | 不建 `docs/` 等子文件夹存放文档（代码文件夹除外，见第 6 节） |

> ⚠️ 第 2 节的决定是用户已经做过的选择。**除非用户主动提出要改，否则不要重新拿出来讨论。**

---

## 3. 技术栈（✅ 已锁定，2026-09-17）

> **用户在 2026-09-17 已就以下方案做出选择。这些是已生效的决定，不要重新讨论。**

| 项目 | 决定 | 说明 |
|---|---|---|
| 框架 | **React 19 + TypeScript** | TypeScript 提供编译期报错，是对非技术用户最重要的保险 |
| 构建工具 | **Vite 8** | 改代码即时刷新；打包产物是纯静态文件 |
| 样式 | **Tailwind CSS v4**（`@tailwindcss/vite` 插件） | v4 不需要 `tailwind.config.js`，配置写在 CSS 里 |
| PWA | **vite-plugin-pwa**，`registerType: 'autoUpdate'` | 自动生成 Service Worker 和 manifest |
| 存储 | **localStorage**，封装在 `storage.ts` | 见 5.6 的切换条件 |
| 测试 | **Vitest**，只测纯逻辑函数，不测界面 | 见产品文档 8.2 |
| 部署 | **GitHub Pages** | 免费、自动 HTTPS；必须 HTTPS 才能用 Service Worker |
| 版本管理 | git，v1 已打标签 `v1.0.0` | 方便回退到可用版本 |

**明确不引入**：路由库、状态管理库、组件库、日期库、图表库、Playwright / Cypress。

### 3.1 用户否决的方案（备查）

| 决定 | 用户选择 | 被否决 | 用户为什么这么选 |
|---|---|---|---|
| 主技术栈 | React + TS + Vite + Tailwind | 单 HTML 文件；Vue / Svelte | 要"写错能提前报错"这层保险；以后加功能容易 |
| 本地存储 | localStorage | IndexedDB | 代码越简单，未来的数据越安全；容量 8–10 年够用 |
| 部署 | GitHub Pages | Vercel / Netlify；只在本机跑 | 免费 + 手机随时能开 + 顺便托管代码 |

### 3.2 已采纳的 8 个产品细节

用户已确认**全部采纳推荐项**：

| # | 决定 |
|---|---|
| ① | 分类**固定**，v1 不支持增删改 |
| ② | 每笔**必须**选到二级小类 |
| ③ | 删除 = 直接删 + 5 秒内可撤销（不做回收站） |
| ④ | 导入备份时**询问**用户，提供「完全覆盖」和「合并」两个按钮，覆盖需二次确认 |
| ⑤ | 日期默认今天，**可以补记**前几天 |
| ⑥ | 列表**显示每日小计** |
| ⑦ | 超过 **30 天**没备份就提醒一次 |
| ⑧ | 金额输入是**纯数字键盘**，不带计算器 |

---

## 4. 决策记录

| 日期 | 决定 | 用户的选择 | 被否决的方案 | 原因 |
|---|---|---|---|---|
| 2026-09-17 | 产品形态 | 网页版 App（PWA） | 原生手机 App / 微信小程序 / 桌面版 | 开发最快、零成本、无需上架审核，电脑手机都能用 |
| 2026-09-17 | 数据存储 | 只存本地 + 手动备份 | 云端账号同步 | 免费、私密、无需服务器；代价是需定期手动导出备份 |
| 2026-09-17 | 记账范围 | 只记支出 | 支出+收入 / 支出+收入+账户 | 最聚焦，界面最简单，最容易坚持 |
| 2026-09-17 | v1 功能范围 | 记流水 + 分类 | 统计图表 / 月度预算 / Excel 导出 | 先做最小可用版本，这些留到 v2 |
| 2026-09-17 | 文件组织 | 全部文件放项目根目录 | 建 `docs/` 子文件夹 | 用户要求所有内容写在 vibe coding 目录下 |
| 2026-09-17 | 主技术栈 | React 19 + TypeScript + Vite 7 + Tailwind v4 | 单 HTML 文件 / Vue / Svelte | 要编译期报错这层保险；以后加功能容易 |
| 2026-09-17 | 本地存储 | localStorage | IndexedDB | 代码简单 = 数据安全；索引优势本项目用不上 |
| 2026-09-17 | 部署方式 | GitHub Pages | Vercel / Netlify / 仅本机 | 免费、手机随时可开、顺便托管代码 |
| 2026-09-17 | 8 个产品细节 | 全部采纳推荐项 | 逐条讨论 | 信任 Claude 的推荐，节省时间 |

---

## 5. 数据模型约定（写代码时必须遵守）

### 5.1 金额

- **一律用整数「分」存储**，字段名 `amountCents`。`¥12.34` → `1234`
- **理由**：小数在计算机里存不精确（`12.34` 实际存成 `12.339999999999999`），按元做加减一年会差几分钱
- 显示时才除以 100。所有换算集中在 `money.ts`，**其他文件不得自己做除法**
- 解析用户输入必须处理：全角数字（`１２．３４`）、全角句点、`¥`/`￥` 符号、首尾空格
- 校验用正则 `^(?:0|[1-9]\d*)(?:\.\d{1,2})?$`，**禁止用 `parseFloat` 直接乘 100**
- 拒绝：空、`0`、负数、超过两位小数、整数部分超过 8 位

### 5.2 日期

- **一律用本地日期字符串 `'YYYY-MM-DD'` 存储**，字段名 `date`
- **理由**：存时间戳会导致"晚上 8 点后记的账被算成第二天"的时区 bug
- 按月份筛选 = 取前 7 位字符串比较；按天分组 = 直接用该字符串当键；排序 = 字符串排序
- 另有 `createdAt` / `updatedAt` 存 ISO 8601 UTC 时间，**仅用于同一天内的排序和排查问题，永远不显示给用户**

### 5.3 分类

- 分类用**稳定 ID**存储（如 `food.takeout`），**不存中文名**
- **理由**：以后改名只需改分类表一行，所有历史记录自动显示新名字
- 备份文件里**同时写入完整分类表**，保证文件"自解释"、记事本可读
- ID 格式：`大类` 或 `大类.小类`，全小写英文 + 下划线
- **ID 一旦发布永不修改**（用户看到的中文名可以改）

### 5.4 备份文件

- 顶层必须有 `app: "pingmian-jizhang"` 和 `schemaVersion` 用于校验
- **导入必须全量校验后再写入**：任何一条不合格就整体拒绝，并指出是第几条出错，**绝不半途写入**
- 导入提供「完全覆盖」和「合并」两种模式，「覆盖」需二次确认

### 5.5 存储 Key 命名

- 一律加 `pmjz:` 前缀和版本后缀，如 `pmjz:expenses:v1`、`pmjz:settings:v1`
- **理由**：将来数据结构变化时可以两代并存、平滑迁移，而不是把旧数据读崩
- 全部账目放在**一个 key** 里。**理由**：一次写入就是原子操作，不会出现"写了一半断电、数据半新半旧"

### 5.6 从 localStorage 换到 IndexedDB 的触发条件

**满足任一条就换**（写死在 `storage.ts` 的注释里）：

1. 数据体积超过 **1.5MB**
2. 记录条数超过 **10,000 条**
3. 要加多设备同步

在触发之前**不要提前优化**。因为所有存储函数的签名都是 `async`，届时只需改 `storage.ts` 一个文件，其他代码一行不动。

---

## 6. 代码规范

- **语言**：界面文案、注释、文档使用**简体中文**；代码标识符（变量名、函数名）用英文
- **注释**：只写"代码本身看不出来的约束"（比如"这里必须用整数分，见 CLAUDE.md 5.1"）。**不要**写"这行是干什么的"这类废话注释
- **文件大小**：单个文件超过约 300 行时，考虑拆分
- **依赖**：**默认不引入新依赖**。每引入一个第三方库都算"技术决策"，必须先问用户（见第一准则）。v1 明确不引入：路由库、状态管理库、组件库、日期库、图表库
- **存储层隔离**：**只有 `storage.ts` 可以直接接触 localStorage**，其他文件一律通过它读写。所有函数签名写成 `async`，为将来换 IndexedDB 留门
- **文件位置**：源码放在 `src/`，组件放在 `src/components/`；文档、配置文件直接放根目录

---

## 7. 常用命令

| 命令 | 作用 | 什么时候用 |
|---|---|---|
| `npm install` | 装依赖 | 第一次，或换了电脑 |
| `npm run dev` | 本地开发模式，改代码即时刷新 | 开发时自己看效果 |
| `npm run build` | 打包成可发布的文件（输出到 `dist/`） | 准备发布前 |
| `npm run preview` | 预览打包后的结果 | ⚠️ **验证"添加到主屏幕"和离线功能必须用这个**，`dev` 模式下这两个功能不生效 |
| `npm run preview -- --host` | 同上，并允许手机通过局域网访问 | 想在真手机上试 |
| `npm test` | 运行自动测试 | 改完金额/日期/分类/备份相关代码后 |
| `npx tsc -b` | 只做类型检查，不打包 | 想快速确认没写错类型 |
| `npm run lint` | 代码风格检查（oxlint） | 提交前 |

**部署到 GitHub Pages**（仓库名假设为 `pingmian-jizhang`）：

```bash
VITE_BASE=/pingmian-jizhang/ npm run build
```

---

## 8. 项目当前状态

**v1 已完成，可运行，68 个自动化测试全部通过。**

| 模块 | 状态 | 测试覆盖 |
|---|---|---|
| 金额换算 `money.ts` | ✅ | 6 组，含全角输入与非法输入 |
| 日期工具 `dates.ts` | ✅ | 4 组，含跨月跨年 |
| 分类表 `categories.ts` | ✅ 12 大类 99 小类（共 111 条） | 11 项完整性检查，含数量锁定 |
| 存储层 `storage.ts` | ✅ | 7 项，用假 localStorage 跑往返 |
| 备份校验 `backup.ts` | ✅ | 13 项，含逐条错误定位 |
| 备份闭环 | ✅ | 3 项，含"导出→导入→再导出内容一致" |
| 状态管理 `useExpenses.ts` | ✅ | 由界面冒烟测试间接覆盖 |
| 界面组件 | ✅ | 15 项渲染冒烟测试（含记账表单、设置面板） |

> 产品设计文档 8.1 节验收清单里的第 14 条（导出→导入→再导出，两个文件内容一致）已由自动化测试覆盖，不必只靠手工验证。

**尚未完成：**

- 任务 15：部署到 GitHub Pages —— **需要用户注册 GitHub 账号并登录**，Claude 不能代劳
- 真实手机上的手动验收（产品设计文档 8.1 节的 20 条清单）

---

## 9. 已知的坑（必须主动提醒用户）

1. **iPhone 上「Safari 网页」和「添加到主屏幕的 App」存储是分开的**，数据不互通。用户必须固定用一种方式记账。
2. **微信内置浏览器不能添加到主屏幕**，且清理微信缓存可能清掉数据。必须用 Safari / Chrome / 系统自带浏览器。
3. **清除浏览器数据 = 清空记账数据。** 导出备份是唯一的救命手段。
4. **`npm run dev` 模式下离线功能不生效**，最容易误判"PWA 坏了"。验证必须用 `npm run preview`。
5. **Service Worker 必须 HTTPS 才能工作**，`file://` 双击打开不行。

---

## 10. 相关文档

| 文档 | 内容 |
|---|---|
| [产品设计文档.md](产品设计文档.md) | 完整产品设计：功能清单、分类体系、界面设计、技术选型对比、验收标准、术语表 |
| CLAUDE.md（本文件） | 工作准则、决策记录、数据模型约定、代码规范 |
