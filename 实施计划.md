# 平面记账 v1 实施计划

> **给执行者：** 必须使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 逐任务执行本计划。步骤用复选框（`- [ ]`）跟踪。

**目标：** 做出一个能在手机浏览器里记账、可添加到主屏幕、可离线使用的极简支出记账 Web App。

**架构：** React 单页应用，无路由、无状态管理库。数据以单个 JSON 字符串存在 localStorage 的 `pmjz:expenses:v1` 键里，所有读写集中在 `storage.ts`（函数签名一律 `async`，为将来换 IndexedDB 留门）。纯逻辑（金额、日期、分类、备份校验）与界面分离，纯逻辑全部有自动测试。

**技术栈：** React 19 + TypeScript + Vite 7 + Tailwind CSS v4 + vite-plugin-pwa + Vitest

**规格来源：** [产品设计文档.md](产品设计文档.md)（产品行为的唯一依据）与 [CLAUDE.md](CLAUDE.md)（工作准则与数据模型约定）。执行前必读这两份。

---

## 全局约束

以下取自 CLAUDE.md，**每个任务都隐含包含这些要求**：

1. **金额一律存整数「分」**，字段 `amountCents`。禁止浮点运算金额，禁止 `parseFloat(x) * 100`。
2. **日期一律存本地日期字符串 `'YYYY-MM-DD'`**，字段 `date`。禁止用时间戳做业务日期。
3. **分类存稳定 ID**（如 `food.takeout`），不存中文名。ID 一旦定下永不修改。
4. **存储键一律带 `pmjz:` 前缀和 `:v1` 版本后缀。**
5. **只有 `storage.ts` 可以直接接触 `localStorage`**，其他文件一律通过它读写。
6. **不引入新依赖**。明确禁止：路由库、状态管理库、组件库、日期库、图表库、Playwright/Cypress。
7. **界面文案用简体中文；代码标识符用英文。**
8. **注释只写"代码本身看不出来的约束"**，不写"这行是干什么的"。
9. **文档、配置文件直接放项目根目录**，不建 `docs/` 之类子文件夹。源码放 `src/`，组件放 `src/components/`。
10. 单个文件超过约 300 行时考虑拆分。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `index.html` | 页面外壳：标题、viewport、iOS 主屏 meta |
| `vite.config.ts` | Vite + Tailwind v4 + PWA 插件 + Vitest 配置 |
| `tsconfig.json` / `tsconfig.node.json` | TypeScript 编译规则 |
| `src/main.tsx` | 入口：挂载 React、请求永久存储、注册 Service Worker |
| `src/App.tsx` | 页面骨架：月份头 + 列表 + 悬浮按钮 + 弹层调度 + 撤销提示 |
| `src/index.css` | Tailwind 引入 + 全局样式（安全区、中文字体栈、禁止选中回弹） |
| `src/types.ts` | 全部类型定义（Expense / Category / Settings / BackupFile） |
| `src/categories.ts` | 12 大类 97 小类数据 + 查询辅助函数 |
| `src/money.ts` | 金额「分 ↔ 元」解析与格式化 |
| `src/dates.ts` | 日期工具：今天、月份键、友好文案 |
| `src/storage.ts` | 唯一接触 localStorage 的文件 |
| `src/backup.ts` | 备份文件校验与导入合并逻辑 |
| `src/useExpenses.ts` | 单一状态源：加载、增删改、按月筛选与合计 |
| `src/components/ExpenseForm.tsx` | 记账表单（新增/编辑共用） |
| `src/components/CategoryPicker.tsx` | 两级分类选择器 |
| `src/components/ExpenseList.tsx` | 按天分组的列表 |
| `src/components/ExpenseItem.tsx` | 单条记录 |
| `src/components/MonthHeader.tsx` | 月份切换 + 本月总支出 |
| `src/components/SettingsSheet.tsx` | 设置面板：备份/恢复/帮助 |
| `src/components/Toast.tsx` | 顶部提示条（撤销、错误、备份提醒复用） |
| `*.test.ts` | 与源码同级，只测纯逻辑 |

---

## 任务 1：项目骨架

**产出：** 能在浏览器里跑起来、手机能打开、装好 PWA 的空页面。

- [ ] **步骤 1：初始化项目**

在 `x:\vibe coding` 下执行（目录已有 CLAUDE.md 等文档，用 `.` 就地初始化）：

```bash
npm create vite@latest . -- --template react-ts
npm install
npm install -D tailwindcss @tailwindcss/vite vite-plugin-pwa vitest
git init
```

若 `npm create vite` 因目录非空而拒绝，改用 `npm create vite@latest pmjz-tmp -- --template react-ts`，把生成内容移入根目录后删掉临时目录。

- [ ] **步骤 2：删除模板残留**

删除 `src/App.css`、`src/assets/react.svg`、`public/vite.svg`；清空 `src/index.css` 内容留待下一步。

- [ ] **步骤 3：配置 `vite.config.ts`**

```ts
/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import { VitePWA } from 'vite-plugin-pwa'

// 部署到 GitHub Pages 的仓库子路径时，用 VITE_BASE 覆盖，例如 VITE_BASE=/pingmian-jizhang/ npm run build
const base = process.env.VITE_BASE ?? '/'

export default defineConfig({
  base,
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['apple-touch-icon.png'],
      manifest: {
        name: '平面记账',
        short_name: '平面记账',
        description: '极简的个人支出记账工具',
        lang: 'zh-CN',
        theme_color: '#0f172a',
        background_color: '#ffffff',
        display: 'standalone',
        orientation: 'portrait',
        start_url: base,
        scope: base,
        icons: [
          { src: 'icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: 'icon-512.png', sizes: '512x512', type: 'image/png' },
          { src: 'icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
        ],
      },
      workbox: {
        globPatterns: ['**/*.{js,css,html,png,svg,woff2}'],
      },
    }),
  ],
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
})
```

- [ ] **步骤 4：配置 `src/index.css`**

```css
@import "tailwindcss";

@theme {
  --font-sans: "PingFang SC", "Microsoft YaHei", "Hiragino Sans GB", system-ui, sans-serif;
}

html, body, #root {
  height: 100%;
}

body {
  /* 手机上地址栏收起时用 100dvh 更准，避免底部按钮被遮住 */
  min-height: 100dvh;
  -webkit-tap-highlight-color: transparent;
  overscroll-behavior-y: none;
}

/* 刘海屏和底部横条的安全区 */
.safe-bottom {
  padding-bottom: max(1rem, env(safe-area-inset-bottom));
}
.safe-top {
  padding-top: max(0.5rem, env(safe-area-inset-top));
}
```

- [ ] **步骤 5：配置 `index.html`**

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, maximum-scale=1.0, user-scalable=no" />
    <meta name="theme-color" content="#0f172a" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="default" />
    <meta name="apple-mobile-web-app-title" content="平面记账" />
    <title>平面记账</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **步骤 6：生成主屏图标**

写一次性脚本 `生成图标.py`（Windows 有 Python 3.13），用 Pillow 画深色圆角方块 + 白色「平」字，导出三张图到 `public/`：

```python
# 运行：python 生成图标.py    （需要 pip install pillow）
from PIL import Image, ImageDraw, ImageFont

BG = (15, 23, 42)      # 深蓝黑，与 theme_color 一致
FG = (255, 255, 255)

def make(size, path, radius_ratio=0.22, font_ratio=0.56):
    img = Image.new("RGB", (size, size), BG)
    d = ImageDraw.Draw(img)
    # 圆角由系统/浏览器裁剪时用 maskable，这里画满底色即可
    font = None
    for name in ("msyhbd.ttc", "msyh.ttc", "simhei.ttf"):
        try:
            font = ImageFont.truetype(f"C:/Windows/Fonts/{name}", int(size * font_ratio))
            break
        except OSError:
            continue
    if font is None:
        raise SystemExit("找不到中文字体，请手动准备图标")
    box = d.textbbox((0, 0), "平", font=font)
    x = (size - (box[2] - box[0])) / 2 - box[0]
    y = (size - (box[3] - box[1])) / 2 - box[1]
    d.text((x, y), "平", font=font, fill=FG)
    img.save(path)

make(192, "public/icon-192.png")
make(512, "public/icon-512.png")
make(180, "public/apple-touch-icon.png", font_ratio=0.56)
```

**兜底**：若 Pillow 装不上，退而求其次——用纯色方块 PNG 占位，并在 README 里记一条"图标待替换"。

- [ ] **步骤 7：写最小 `src/App.tsx` 和 `src/main.tsx` 验证能跑**

`src/main.tsx`：

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App'

// 降低浏览器在磁盘紧张时清掉账目数据的概率
if (navigator.storage?.persist) {
  navigator.storage.persist().catch(() => {})
}

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

`src/App.tsx` 暂时只渲染 `<main className="p-4">平面记账</main>`。

- [ ] **步骤 8：验证**

```bash
npm run dev
```
浏览器打开，看到"平面记账"四个字，无报错。然后 `npm run build` 应成功。

- [ ] **步骤 9：提交**

```bash
git add -A && git commit -m "chore: 搭建 React + Vite + Tailwind + PWA 项目骨架"
```

---

## 任务 2：金额模块 `src/money.ts`

**产出：** 金额解析与格式化函数，全部有测试。

**接口：**
- 产出：`parseAmountToCents(raw: string): number | null`、`formatCents(cents: number): string`

- [ ] **步骤 1：写失败的测试 `src/money.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { parseAmountToCents, formatCents } from './money'

describe('parseAmountToCents', () => {
  it('解析普通金额', () => {
    expect(parseAmountToCents('12.34')).toBe(1234)
    expect(parseAmountToCents('0.01')).toBe(1)
    expect(parseAmountToCents('1000')).toBe(100000)
    expect(parseAmountToCents('12.3')).toBe(1230)
    expect(parseAmountToCents('12.30')).toBe(1230)
  })

  it('容忍全角数字和全角句点（中文输入法常见）', () => {
    expect(parseAmountToCents('１２．３４')).toBe(1234)
  })

  it('容忍货币符号和空格', () => {
    expect(parseAmountToCents(' ¥12.34 ')).toBe(1234)
    expect(parseAmountToCents('￥12.34')).toBe(1234)
    expect(parseAmountToCents('¥ 12.34')).toBe(1234)
  })

  it('拒绝非法输入', () => {
    for (const bad of ['', '   ', 'abc', '0', '0.00', '-5', '1.234', '1e5', '.5', '12.', '1,000', 'NaN', 'Infinity']) {
      expect(parseAmountToCents(bad), `应拒绝 ${JSON.stringify(bad)}`).toBeNull()
    }
  })

  it('拒绝超过 8 位整数部分', () => {
    expect(parseAmountToCents('12345678')).toBe(1234567800)
    expect(parseAmountToCents('123456789')).toBeNull()
  })
})

describe('formatCents', () => {
  it('格式化为两位小数', () => {
    expect(formatCents(1234)).toBe('12.34')
    expect(formatCents(1)).toBe('0.01')
    expect(formatCents(0)).toBe('0.00')
    expect(formatCents(100000)).toBe('1000.00')
  })

  it('整数相加不丢分', () => {
    expect(formatCents(1234 + 2)).toBe('12.36')
  })
})
```

- [ ] **步骤 2：运行测试确认失败**

```bash
npx vitest run src/money.test.ts
```
预期：FAIL，报 `Failed to resolve import "./money"`。

- [ ] **步骤 3：实现 `src/money.ts`**

```ts
/** 单笔金额上限：整数部分 8 位，防止误输入天文数字撑爆统计 */
const MAX_INT_DIGITS = 8

/** 全角数字、全角句点、全角逗号 → 半角。中文输入法下很容易打出来 */
function toHalfWidth(s: string): string {
  return s
    .replace(/[\uFF10-\uFF19]/g, (c) => String.fromCharCode(c.charCodeAt(0) - 0xfee0))
    .replace(/\uFF0E/g, '.')
    .replace(/\uFF0C/g, ',')
}

/**
 * 把用户输入解析成整数「分」。非法输入返回 null。
 * 不用 parseFloat：12.34 * 100 在浮点数下是 1233.9999999999998。
 */
export function parseAmountToCents(raw: string): number | null {
  const s = toHalfWidth(raw).replace(/[¥￥\s,]/g, '')
  if (!/^(?:0|[1-9]\d*)(?:\.\d{1,2})?$/.test(s)) return null
  const [intPart, decPart = ''] = s.split('.')
  if (intPart.length > MAX_INT_DIGITS) return null
  const cents = Number(intPart) * 100 + Number(decPart.padEnd(2, '0') || '0')
  return cents > 0 ? cents : null
}

/** 把「分」格式化成 '12.34'。不做千分位，列表里数字更紧凑好扫 */
export function formatCents(cents: number): string {
  const sign = cents < 0 ? '-' : ''
  const abs = Math.abs(Math.round(cents))
  return `${sign}${Math.floor(abs / 100)}.${String(abs % 100).padStart(2, '0')}`
}
```

- [ ] **步骤 4：运行测试确认通过**

```bash
npx vitest run src/money.test.ts
```
预期：全部 PASS。

- [ ] **步骤 5：提交**

```bash
git add src/money.ts src/money.test.ts && git commit -m "feat: 金额分/元换算，全角输入容错"
```

---

## 任务 3：日期模块 `src/dates.ts`

**产出：** 日期工具函数，全部有测试。

**接口：**
- 产出：`todayStr(): string`、`monthKeyOf(date: string): string`、`monthLabel(monthKey: string): string`、`dayLabel(date: string, today: string): string`、`shiftMonth(monthKey: string, delta: number): string`

- [ ] **步骤 1：写失败的测试 `src/dates.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { monthKeyOf, monthLabel, dayLabel, shiftMonth } from './dates'

describe('monthKeyOf', () => {
  it('取前 7 位作为月份键', () => {
    expect(monthKeyOf('2026-09-17')).toBe('2026-09')
  })
})

describe('shiftMonth', () => {
  it('普通前后移动', () => {
    expect(shiftMonth('2026-09', 1)).toBe('2026-10')
    expect(shiftMonth('2026-09', -1)).toBe('2026-08')
  })

  it('跨年前后移动', () => {
    expect(shiftMonth('2026-12', 1)).toBe('2027-01')
    expect(shiftMonth('2026-01', -1)).toBe('2025-12')
  })

  it('跨多年', () => {
    expect(shiftMonth('2026-03', 25)).toBe('2028-04')
    expect(shiftMonth('2026-03', -25)).toBe('2024-02')
  })
})

describe('monthLabel', () => {
  it('输出中文年月', () => {
    expect(monthLabel('2026-09')).toBe('2026 年 9 月')
  })
})

describe('dayLabel', () => {
  const today = '2026-09-17'
  it('今天和昨天用友好文案', () => {
    expect(dayLabel('2026-09-17', today)).toBe('今天')
    expect(dayLabel('2026-09-16', today)).toBe('昨天')
  })
  it('更早的日期显示月日和星期', () => {
    expect(dayLabel('2026-09-15', today)).toBe('9 月 15 日 周二')
  })
  it('跨月的昨天也能正确识别', () => {
    expect(dayLabel('2026-08-31', '2026-09-01')).toBe('昨天')
  })
  it('跨年的昨天也能正确识别', () => {
    expect(dayLabel('2025-12-31', '2026-01-01')).toBe('昨天')
  })
})
```

- [ ] **步骤 2：运行测试确认失败**

```bash
npx vitest run src/dates.test.ts
```
预期：FAIL，`Failed to resolve import "./dates"`。

- [ ] **步骤 3：实现 `src/dates.ts`**

```ts
const WEEKDAYS = ['周日', '周一', '周二', '周三', '周四', '周五', '周六'] as const

/**
 * 取本地日期字符串 'YYYY-MM-DD'。
 * 不用 toISOString()：它转成 UTC，东八区晚上 8 点后会算成第二天。
 */
export function todayStr(): string {
  const d = new Date()
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`
}

/** '2026-09-17' → '2026-09'。日期是等宽补零字符串，取前 7 位即可 */
export function monthKeyOf(date: string): string {
  return date.slice(0, 7)
}

/** '2026-09' → '2026 年 9 月' */
export function monthLabel(monthKey: string): string {
  const [y, m] = monthKey.split('-')
  return `${y} 年 ${Number(m)} 月`
}

/** 构造本地零点的 Date，避免用 new Date('2026-09-17') 被解析成 UTC */
function parseLocalDate(date: string): Date {
  const [y, m, d] = date.split('-').map(Number)
  return new Date(y, m - 1, d)
}

/** 在月份键上前后移动。用 Date 做进位，自动处理跨年 */
export function shiftMonth(monthKey: string, delta: number): string {
  const [y, m] = monthKey.split('-').map(Number)
  const d = new Date(y, m - 1 + delta, 1)
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`
}

/** 列表里的日期标题：今天 / 昨天 / 9 月 15 日 周二 */
export function dayLabel(date: string, today: string): string {
  if (date === today) return '今天'
  if (date === previousDay(today)) return '昨天'
  const d = parseLocalDate(date)
  return `${d.getMonth() + 1} 月 ${d.getDate()} 日 ${WEEKDAYS[d.getDay()]}`
}

function previousDay(date: string): string {
  const d = parseLocalDate(date)
  d.setDate(d.getDate() - 1)
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`
}
```

- [ ] **步骤 4：运行测试确认通过**

```bash
npx vitest run src/dates.test.ts
```
预期：全部 PASS。若"9 月 15 日 周二"断言不符，用真实星期几修正测试（2026-09-15 是周二）。

- [ ] **步骤 5：提交**

```bash
git add src/dates.ts src/dates.test.ts && git commit -m "feat: 本地日期工具，避免时区跨界错误"
```

---

## 任务 4：类型与分类表 `src/types.ts` + `src/categories.ts`

**产出：** 全部类型定义 + 12 大类 97 小类的完整数据 + 完整性测试。

**接口：**
- 产出：
  - `Expense`、`Category`、`CategoryId`、`Settings`、`BackupFile`（types.ts）
  - `CATEGORIES: Category[]`、`TOP_CATEGORIES: Category[]`、`subcategoriesOf(parentId): Category[]`、`categoryById(id): Category | undefined`、`categoryPath(id): string`
  - `APP_ID = 'pingmian-jizhang'`、`SCHEMA_VERSION = 1`

- [ ] **步骤 1：写 `src/types.ts`**

```ts
/** 分类的稳定标识，例如 'food'、'food.takeout'。永不修改 */
export type CategoryId = string

/** 一条支出记录 */
export interface Expense {
  id: string
  /** 金额，单位「分」，必须是整数。¥12.34 → 1234 */
  amountCents: number
  categoryId: CategoryId
  subcategoryId: CategoryId
  /** 备注，没有时存空字符串，不存 undefined */
  note: string
  /** 记账日期，本地日期 'YYYY-MM-DD' */
  date: string
  /** 录入时间 ISO 8601 UTC，仅用于同日内排序，不显示给用户 */
  createdAt: string
  updatedAt: string
}

export interface Category {
  id: CategoryId
  /** 显示名。改名只改这里，历史记录会自动跟着变 */
  name: string
  /** null 表示一级大类 */
  parentId: CategoryId | null
  sortOrder: number
}

export interface Settings {
  schemaVersion: number
  lastBackupAt: string | null
}

export interface BackupFile {
  app: string
  schemaVersion: number
  exportedAt: string
  categories: Category[]
  expenses: Expense[]
}

export const APP_ID = 'pingmian-jizhang'
export const SCHEMA_VERSION = 1
```

- [ ] **步骤 2：写 `src/categories.ts`**

按 [产品设计文档.md](产品设计文档.md) 第 4 节的表格逐条录入，`sortOrder` 用 10 的倍数（留空位方便以后插入）。结构如下（**必须录全 12 个大类，二级按文档表格逐条录完**）：

```ts
import type { Category, CategoryId } from './types'

const top = (id: CategoryId, name: string, sortOrder: number): Category => ({ id, name, parentId: null, sortOrder })
const sub = (parentId: CategoryId, id: CategoryId, name: string, sortOrder: number): Category => ({ id, name, parentId, sortOrder })

export const CATEGORIES: Category[] = [
  top('food', '餐饮', 10),
  sub('food', 'food.breakfast', '早餐', 10),
  sub('food', 'food.lunch', '午餐', 20),
  sub('food', 'food.dinner', '晚餐', 30),
  sub('food', 'food.late_night', '夜宵', 40),
  sub('food', 'food.takeout', '外卖', 50),
  sub('food', 'food.groceries', '买菜做饭', 60),
  sub('food', 'food.snacks', '零食饮料', 70),
  sub('food', 'food.fruit', '水果', 80),
  sub('food', 'food.coffee_tea', '咖啡奶茶', 90),
  sub('food', 'food.tobacco_alcohol', '烟酒', 100),
  sub('food', 'food.dining_out', '聚餐下馆子', 110),

  // ... 其余 11 个大类按产品设计文档第 4 节逐条录入：
  // transport(11) shopping(9) housing(10) comm(5) medical(9)
  // education(9) ent(8) social(7) services(8) pets(7) other(5)
]

/** 一级大类，按 sortOrder 排好 */
export const TOP_CATEGORIES: Category[] = CATEGORIES
  .filter((c) => c.parentId === null)
  .sort((a, b) => a.sortOrder - b.sortOrder)

const byId = new Map(CATEGORIES.map((c) => [c.id, c]))

export function categoryById(id: CategoryId): Category | undefined {
  return byId.get(id)
}

/** 某个大类下的二级小类，按 sortOrder 排好 */
export function subcategoriesOf(parentId: CategoryId): Category[] {
  return CATEGORIES.filter((c) => c.parentId === parentId).sort((a, b) => a.sortOrder - b.sortOrder)
}

/** '餐饮 · 外卖'，用于列表显示。分类被删时优雅降级而不是崩溃 */
export function categoryPath(subcategoryId: CategoryId): string {
  const s = byId.get(subcategoryId)
  if (!s) return '未知分类'
  const p = s.parentId ? byId.get(s.parentId) : undefined
  return p ? `${p.name} · ${s.name}` : s.name
}
```

- [ ] **步骤 3：写失败的完整性测试 `src/categories.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { CATEGORIES, TOP_CATEGORIES, subcategoriesOf, categoryPath } from './categories'

describe('分类表完整性', () => {
  it('ID 全局唯一', () => {
    const ids = CATEGORIES.map((c) => c.id)
    expect(new Set(ids).size).toBe(ids.length)
  })

  it('每个二级小类的 parentId 都指向一个存在的一级大类', () => {
    const topIds = new Set(TOP_CATEGORIES.map((c) => c.id))
    for (const c of CATEGORIES) {
      if (c.parentId !== null) {
        expect(topIds.has(c.parentId), `${c.id} 的父类 ${c.parentId} 不存在`).toBe(true)
      }
    }
  })

  it('二级小类的 ID 以父类 ID 加点开头', () => {
    for (const c of CATEGORIES) {
      if (c.parentId !== null) expect(c.id.startsWith(`${c.parentId}.`)).toBe(true)
    }
  })

  it('每个一级大类至少有 4 个二级小类', () => {
    for (const t of TOP_CATEGORIES) {
      expect(subcategoriesOf(t.id).length, `${t.name} 的小类太少`).toBeGreaterThanOrEqual(4)
    }
  })

  it('恰好 12 个一级大类', () => {
    expect(TOP_CATEGORIES.length).toBe(12)
  })

  it('没有空名字', () => {
    for (const c of CATEGORIES) expect(c.name.trim().length).toBeGreaterThan(0)
  })

  it('同一父类下的小类 sortOrder 不重复', () => {
    for (const t of TOP_CATEGORIES) {
      const orders = subcategoriesOf(t.id).map((c) => c.sortOrder)
      expect(new Set(orders).size, `${t.name} 下有 sortOrder 撞车`).toBe(orders.length)
    }
  })

  it('categoryPath 输出两级名字', () => {
    expect(categoryPath('food.takeout')).toBe('餐饮 · 外卖')
  })

  it('categoryPath 遇到未知 ID 不崩溃', () => {
    expect(categoryPath('nope.nope')).toBe('未知分类')
  })
})
```

- [ ] **步骤 4：运行测试**

```bash
npx vitest run src/categories.test.ts
```
先确认 FAIL（分类还没录全时会挂在"恰好 12 个一级大类"上），录完后应全部 PASS。

- [ ] **步骤 5：提交**

```bash
git add src/types.ts src/categories.ts src/categories.test.ts && git commit -m "feat: 类型定义与 12 大类分类表"
```

---

## 任务 5：存储层 `src/storage.ts`

**产出：** 唯一的 localStorage 访问点。

**接口：**
- 消费：`Expense`、`Settings`、`SCHEMA_VERSION`（types.ts）
- 产出：`loadExpenses(): Promise<Expense[]>`、`saveExpenses(list): Promise<void>`、`loadSettings(): Promise<Settings>`、`saveSettings(s): Promise<void>`、`estimateUsageBytes(): Promise<number>`、`clearAll(): Promise<void>`

- [ ] **步骤 1：实现 `src/storage.ts`**

```ts
import type { Expense, Settings } from './types'
import { SCHEMA_VERSION } from './types'

const EXPENSES_KEY = 'pmjz:expenses:v1'
const SETTINGS_KEY = 'pmjz:settings:v1'

/** 所有函数写成 async 是刻意的：将来数据超过 1.5MB / 10000 条、或要做多设备同步时，
 *  只需把本文件换成 IndexedDB 实现，调用方一行不用改。见 CLAUDE.md 6.6 */

const DEFAULT_SETTINGS: Settings = { schemaVersion: SCHEMA_VERSION, lastBackupAt: null }

function readRaw(key: string): string | null {
  try {
    return localStorage.getItem(key)
  } catch {
    return null
  }
}

function writeRaw(key: string, value: string): void {
  // 容量不足时 QuotaExceededError 必须抛给调用方，绝不静默失败
  localStorage.setItem(key, value)
}

export async function loadExpenses(): Promise<Expense[]> {
  const raw = readRaw(EXPENSES_KEY)
  if (!raw) return []
  try {
    const parsed = JSON.parse(raw)
    return Array.isArray(parsed) ? parsed : []
  } catch {
    // 数据损坏时退回空列表，绝不能让整个 App 打不开
    console.error('账目数据解析失败，已按空列表处理')
    return []
  }
}

export async function saveExpenses(list: Expense[]): Promise<void> {
  writeRaw(EXPENSES_KEY, JSON.stringify(list))
}

export async function loadSettings(): Promise<Settings> {
  const raw = readRaw(SETTINGS_KEY)
  if (!raw) return { ...DEFAULT_SETTINGS }
  try {
    return { ...DEFAULT_SETTINGS, ...JSON.parse(raw) }
  } catch {
    return { ...DEFAULT_SETTINGS }
  }
}

export async function saveSettings(s: Settings): Promise<void> {
  writeRaw(SETTINGS_KEY, JSON.stringify(s))
}

/** 估算已用体积，用于"数据快满了"的提醒 */
export async function estimateUsageBytes(): Promise<number> {
  return new Blob([readRaw(EXPENSES_KEY) ?? '']).size
}

export async function clearAll(): Promise<void> {
  localStorage.removeItem(EXPENSES_KEY)
  localStorage.removeItem(SETTINGS_KEY)
}
```

- [ ] **步骤 2：类型检查通过**

```bash
npx tsc --noEmit
```
预期：无错误。

- [ ] **步骤 3：提交**

```bash
git add src/storage.ts && git commit -m "feat: localStorage 存储层，异步签名预留切换空间"
```

---

## 任务 6：备份校验与导入 `src/backup.ts`

**产出：** 备份文件的构造、校验与合并逻辑，全部有测试。**这是数据丢失时唯一的救命通道，测试最不能省。**

**接口：**
- 消费：`Expense`、`Category`、`BackupFile`、`APP_ID`、`SCHEMA_VERSION`（types.ts）；`CATEGORIES`（categories.ts）
- 产出：`buildBackup(expenses: Expense[]): BackupFile`、`validateBackup(input: unknown): { ok: true; value: BackupFile } | { ok: false; error: string }`、`mergeExpenses(current: Expense[], incoming: Expense[]): Expense[]`、`backupFileName(dateStr: string): string`

- [ ] **步骤 1：写失败的测试 `src/backup.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { validateBackup, mergeExpenses, buildBackup, backupFileName } from './backup'
import type { Expense } from './types'

const good = (over: Partial<Expense> = {}): Expense => ({
  id: 'a1',
  amountCents: 1234,
  categoryId: 'food',
  subcategoryId: 'food.takeout',
  note: '',
  date: '2026-09-17',
  createdAt: '2026-09-17T04:00:00.000Z',
  updatedAt: '2026-09-17T04:00:00.000Z',
  ...over,
})

describe('buildBackup', () => {
  it('带上 App 标识、版本号和完整分类表', () => {
    const b = buildBackup([good()])
    expect(b.app).toBe('pingmian-jizhang')
    expect(b.schemaVersion).toBe(1)
    expect(b.categories.length).toBeGreaterThan(90)
    expect(b.expenses).toHaveLength(1)
  })
})

describe('validateBackup', () => {
  it('接受合法文件', () => {
    const r = validateBackup(buildBackup([good()]))
    expect(r.ok).toBe(true)
  })

  it('拒绝不是本 App 的文件', () => {
    const r = validateBackup({ ...buildBackup([]), app: 'other' })
    expect(r.ok).toBe(false)
  })

  it('拒绝 expenses 不是数组', () => {
    const r = validateBackup({ ...buildBackup([]), expenses: 'nope' })
    expect(r.ok).toBe(false)
  })

  it('拒绝未来版本号', () => {
    const r = validateBackup({ ...buildBackup([]), schemaVersion: 999 })
    expect(r.ok).toBe(false)
  })

  it('拒绝金额是非整数', () => {
    const r = validateBackup({ ...buildBackup([]), expenses: [{ ...good(), amountCents: 12.5 }] })
    expect(r.ok).toBe(false)
  })

  it('拒绝金额是字符串', () => {
    const r = validateBackup({ ...buildBackup([]), expenses: [{ ...good(), amountCents: 'abc' }] })
    expect(r.ok).toBe(false)
  })

  it('拒绝日期格式不符', () => {
    const r = validateBackup({ ...buildBackup([]), expenses: [{ ...good(), date: '2026/09/17' }] })
    expect(r.ok).toBe(false)
  })

  it('报错信息里指出是第几条出错', () => {
    const r = validateBackup({ ...buildBackup([]), expenses: [good({ id: 'x' }), { ...good(), amountCents: -1 }] })
    expect(r.ok).toBe(false)
    if (!r.ok) expect(r.error).toContain('第 2 条')
  })

  it('拒绝非对象输入', () => {
    expect(validateBackup(null).ok).toBe(false)
    expect(validateBackup('{}').ok).toBe(false)
  })
})

describe('mergeExpenses', () => {
  it('按 id 去重，新记录覆盖同 id 的旧记录', () => {
    const current = [good({ id: 'a', amountCents: 100 })]
    const incoming = [good({ id: 'a', amountCents: 999 }), good({ id: 'b' })]
    const merged = mergeExpenses(current, incoming)
    expect(merged).toHaveLength(2)
    expect(merged.find((e) => e.id === 'a')!.amountCents).toBe(999)
  })
})

describe('backupFileName', () => {
  it('文件名带日期，便于区分', () => {
    expect(backupFileName('2026-09-17')).toBe('平面记账-备份-2026-09-17.json')
  })
})
```

- [ ] **步骤 2：运行确认失败**

```bash
npx vitest run src/backup.test.ts
```

- [ ] **步骤 3：实现 `src/backup.ts`**

```ts
import type { BackupFile, Expense } from './types'
import { APP_ID, SCHEMA_VERSION } from './types'
import { CATEGORIES } from './categories'

const DATE_RE = /^\d{4}-\d{2}-\d{2}$/

export function buildBackup(expenses: Expense[]): BackupFile {
  return {
    app: APP_ID,
    schemaVersion: SCHEMA_VERSION,
    exportedAt: new Date().toISOString(),
    categories: CATEGORIES,
    expenses,
  }
}

export function backupFileName(dateStr: string): string {
  return `平面记账-备份-${dateStr}.json`
}

export type ValidationResult = { ok: true; value: BackupFile } | { ok: false; error: string }

function checkExpense(e: unknown, index: number): string | null {
  const at = `第 ${index + 1} 条`
  if (typeof e !== 'object' || e === null) return `${at}不是一条有效记录`
  const r = e as Record<string, unknown>
  if (typeof r.id !== 'string' || !r.id) return `${at}缺少编号`
  if (!Number.isInteger(r.amountCents)) return `${at}的金额不是整数分`
  if ((r.amountCents as number) <= 0) return `${at}的金额必须大于 0`
  if (typeof r.categoryId !== 'string' || !r.categoryId) return `${at}缺分类`
  if (typeof r.subcategoryId !== 'string' || !r.subcategoryId) return `${at}缺子分类`
  if (typeof r.note !== 'string') return `${at}的备注格式不对`
  if (typeof r.date !== 'string' || !DATE_RE.test(r.date)) return `${at}的日期格式不是 YYYY-MM-DD`
  if (typeof r.createdAt !== 'string') return `${at}缺少录入时间`
  if (typeof r.updatedAt !== 'string') return `${at}缺少修改时间`
  return null
}

/** 全量校验后才允许写入。任何一条不合格就整体拒绝，绝不半途写入 */
export function validateBackup(input: unknown): ValidationResult {
  if (typeof input !== 'object' || input === null || Array.isArray(input)) {
    return { ok: false, error: '这不是一个备份文件' }
  }
  const b = input as Record<string, unknown>
  if (b.app !== APP_ID) return { ok: false, error: '这不是「平面记账」的备份文件' }
  if (!Number.isInteger(b.schemaVersion)) return { ok: false, error: '备份文件缺少版本号' }
  if ((b.schemaVersion as number) > SCHEMA_VERSION) {
    return { ok: false, error: '备份文件来自更新版本的 App，请先升级再导入' }
  }
  if (!Array.isArray(b.expenses)) return { ok: false, error: '备份文件里的账目不是一个列表' }
  for (let i = 0; i < b.expenses.length; i++) {
    const err = checkExpense(b.expenses[i], i)
    if (err) return { ok: false, error: err }
  }
  return { ok: true, value: b as unknown as BackupFile }
}

/** 合并：以 id 为准，导入的记录覆盖同 id 的现有记录 */
export function mergeExpenses(current: Expense[], incoming: Expense[]): Expense[] {
  const map = new Map(current.map((e) => [e.id, e]))
  for (const e of incoming) map.set(e.id, e)
  return [...map.values()]
}
```

- [ ] **步骤 4：运行确认通过**

```bash
npx vitest run src/backup.test.ts
```
预期：全部 PASS。

- [ ] **步骤 5：提交**

```bash
git add src/backup.ts src/backup.test.ts && git commit -m "feat: 备份文件构造、全量校验与合并"
```

---

## 任务 7：状态源 `src/useExpenses.ts`

**产出：** 串起加载、增删改、按月筛选与合计的单一状态源。

**接口：**
- 消费：`storage.ts` 全部函数；`todayStr`、`monthKeyOf`（dates.ts）
- 产出：`useExpenses()` 返回 `{ loading, error, expenses, addExpense, updateExpense, removeExpense, restoreExpense, replaceAll, mergeIn, monthTotal }`

- [ ] **步骤 1：实现 `src/useExpenses.ts`**

```ts
import { useCallback, useEffect, useMemo, useState } from 'react'
import type { Expense } from './types'
import * as storage from './storage'
import { monthKeyOf } from './dates'

export interface NewExpense {
  amountCents: number
  categoryId: string
  subcategoryId: string
  note: string
  date: string
}

/** 全局排序：日期倒序，同一天内后记的排在上面 */
function sortExpenses(list: Expense[]): Expense[] {
  return [...list].sort((a, b) => (a.date === b.date ? b.createdAt.localeCompare(a.createdAt) : b.date.localeCompare(a.date)))
}

export function useExpenses() {
  const [expenses, setExpenses] = useState<Expense[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    storage
      .loadExpenses()
      .then((list) => setExpenses(sortExpenses(list)))
      .finally(() => setLoading(false))
  }, [])

  /** 唯一写入口。写入失败（例如存储空间满）时把错误暴露给界面 */
  const commit = useCallback(async (next: Expense[]) => {
    const sorted = sortExpenses(next)
    try {
      await storage.saveExpenses(sorted)
      setExpenses(sorted)
      setError(null)
      return true
    } catch {
      setError('保存失败：手机存储空间可能已满。请先导出备份，再清理一些数据。')
      return false
    }
  }, [])

  const addExpense = useCallback(
    async (input: NewExpense) => {
      const now = new Date().toISOString()
      const record: Expense = { id: crypto.randomUUID(), ...input, createdAt: now, updatedAt: now }
      return commit([...expenses, record])
    },
    [commit, expenses],
  )

  const updateExpense = useCallback(
    async (id: string, input: NewExpense) => {
      return commit(expenses.map((e) => (e.id === id ? { ...e, ...input, updatedAt: new Date().toISOString() } : e)))
    },
    [commit, expenses],
  )

  const removeExpense = useCallback(async (id: string) => commit(expenses.filter((e) => e.id !== id)), [commit, expenses])

  /** 撤销删除：把整条记录放回去 */
  const restoreExpense = useCallback(
    async (record: Expense) => {
      if (expenses.some((e) => e.id === record.id)) return true
      return commit([...expenses, record])
    },
    [commit, expenses],
  )

  const replaceAll = useCallback(async (list: Expense[]) => commit(list), [commit])

  const mergeIn = useCallback(
    async (incoming: Expense[]) => {
      const map = new Map(expenses.map((e) => [e.id, e]))
      for (const e of incoming) map.set(e.id, e)
      return commit([...map.values()])
    },
    [commit, expenses],
  )

  /** 按月合计。整数分相加，不会丢精度 */
  const monthTotal = useCallback(
    (monthKey: string) =>
      expenses.reduce((sum, e) => (monthKeyOf(e.date) === monthKey ? sum + e.amountCents : sum), 0),
    [expenses],
  )

  const byMonth = useMemo(() => {
    const groups = new Map<string, Expense[]>()
    for (const e of expenses) {
      const k = monthKeyOf(e.date)
      const arr = groups.get(k)
      if (arr) arr.push(e)
      else groups.set(k, [e])
    }
    return groups
  }, [expenses])

  return { loading, error, expenses, byMonth, addExpense, updateExpense, removeExpense, restoreExpense, replaceAll, mergeIn, monthTotal }
}
```

- [ ] **步骤 2：类型检查**

```bash
npx tsc --noEmit
```
预期：无错误。

- [ ] **步骤 3：提交**

```bash
git add src/useExpenses.ts && git commit -m "feat: 账目状态管理，含增删改、撤销与按月合计"
```

---

## 任务 8：通用提示条 `src/components/Toast.tsx`

**产出：** 顶部提示条，撤销、错误、备份提醒三处复用。

**接口：**
- 产出：`<Toast message tone actionLabel onAction onDismiss />`

- [ ] **步骤 1：实现**

```tsx
interface ToastProps {
  message: string
  /** info 用于备份提醒，error 用于保存失败，success 用于操作确认 */
  tone?: 'info' | 'error' | 'success'
  actionLabel?: string
  onAction?: () => void
  onDismiss: () => void
}

const TONE = {
  info: 'bg-slate-800',
  error: 'bg-red-600',
  success: 'bg-emerald-600',
} as const

export default function Toast({ message, tone = 'info', actionLabel, onAction, onDismiss }: ToastProps) {
  return (
    <div className="pointer-events-none fixed inset-x-0 top-0 z-50 flex justify-center px-3 safe-top">
      <div className={`pointer-events-auto flex w-full max-w-md items-center gap-3 rounded-xl px-4 py-3 text-sm text-white shadow-lg ${TONE[tone]}`}>
        <span className="flex-1">{message}</span>
        {actionLabel && onAction && (
          <button type="button" onClick={onAction} className="shrink-0 font-semibold underline">
            {actionLabel}
          </button>
        )}
        <button type="button" onClick={onDismiss} aria-label="关闭" className="shrink-0 px-1 text-white/70">
          ✕
        </button>
      </div>
    </div>
  )
}
```

- [ ] **步骤 2：提交**

```bash
git add src/components/Toast.tsx && git commit -m "feat: 顶部提示条组件"
```

---

## 任务 9：两级分类选择器 `src/components/CategoryPicker.tsx`

**产出：** 先点大类、再点小类的选择器。二级必选（见决定 ②）。

**接口：**
- 消费：`TOP_CATEGORIES`、`subcategoriesOf`（categories.ts）
- 产出：`<CategoryPicker value={{ categoryId, subcategoryId }} onChange={(v) => void} />`

- [ ] **步骤 1：实现**

要点：
- 上半区横向排列 12 个一级大类按钮，当前选中的高亮
- 下半区显示所选大类下的二级小类按钮；**点击小类即回调 `onChange({ categoryId, subcategoryId })` 并自动收起**
- 大类图标用"首字方块"：取 `name[0]` 放进圆角方块，不引图标库
- 未选大类时不显示小类区，显示一行浅色提示"先选一个大类"

```tsx
import { useState } from 'react'
import { TOP_CATEGORIES, subcategoriesOf } from '../categories'

interface Props {
  value: { categoryId: string; subcategoryId: string }
  onChange: (v: { categoryId: string; subcategoryId: string }) => void
}

/** 首字方块：零成本、风格统一，不引图标库 */
export function CategoryBadge({ name, active }: { name: string; active: boolean }) {
  return (
    <span className={`flex h-7 w-7 items-center justify-center rounded-lg text-xs font-semibold ${active ? 'bg-white/20' : 'bg-slate-100 text-slate-500'}`}>
      {name.slice(0, 1)}
    </span>
  )
}
```

（完整实现按上述要点展开：`useState` 保存当前展开的大类，初始值取 `value.categoryId`。）

- [ ] **步骤 2：类型检查并提交**

```bash
npx tsc --noEmit && git add src/components/CategoryPicker.tsx && git commit -m "feat: 两级分类选择器"
```

---

## 任务 10：记账表单 `src/components/ExpenseForm.tsx`

**产出：** 新增与编辑共用的记账表单。

**接口：**
- 消费：`parseAmountToCents`（money.ts）、`todayStr`（dates.ts）、`CategoryPicker`
- 产出：`<ExpenseForm initial={Expense | null} onSubmit={(v: NewExpense) => Promise<boolean>} onCancel={() => void} onDelete?: (id: string) => void />`

- [ ] **步骤 1：实现**

要点：
- 金额输入用 `<input inputMode="decimal" autoFocus>`，字号要大（`text-4xl`），左前置 `¥`
- 用 `parseAmountToCents` 校验，返回 `null` 时禁用保存按钮并显示"请输入正确的金额"
- 分类区嵌入 `CategoryPicker`
- 备注用 `<input maxLength={200}>`
- 日期默认 `todayStr()`，提供「今天 / 昨天 / 前天」三个快捷按钮，另外允许 `<input type="date">` 手选
- 编辑模式下底部多一个「删除」按钮
- `onSubmit` 返回 `false`（保存失败）时**不关闭表单**，避免用户输入白费

- [ ] **步骤 2：类型检查并提交**

```bash
npx tsc --noEmit && git add src/components/ExpenseForm.tsx && git commit -m "feat: 记账表单（新增/编辑共用）"
```

---

## 任务 11：列表与月份头 `src/components/ExpenseList.tsx` + `ExpenseItem.tsx` + `MonthHeader.tsx`

**产出：** 按天分组、带每日小计和本月总支出的主界面主体。

**接口：**
- 消费：`dayLabel`、`todayStr`、`monthLabel`、`shiftMonth`、`formatCents`、`categoryPath`
- 产出：`<MonthHeader monthKey onShift={(d: number) => void} totalCents={number} />`、`<ExpenseList expenses={Expense[]} onSelect={(e: Expense) => void} />`

- [ ] **步骤 1：实现 `MonthHeader`**

左侧 `‹`、中间 `monthLabel(monthKey)`、右侧 `›`；下方大字显示 `¥ {formatCents(total)}`，标签"本月支出"。`‹`/`›` 按钮用 `onShift(-1)` / `onShift(1)`。

- [ ] **步骤 2：实现 `ExpenseItem`**

一行：左侧 `CategoryBadge` + `categoryPath(subcategoryId)`（主行）和备注（次行，浅灰，为空时不渲染）；右侧金额 `-¥{formatCents(amountCents)}`。整行可点，点击回调 `onSelect(expense)` 打开编辑。

- [ ] **步骤 3：实现 `ExpenseList`**

按 `date` 分组（输入已按日期倒序排好，顺序遍历即可）。每组渲染一个粘性日期标题行：左边 `dayLabel(date, todayStr())`，右边该日小计 `¥{formatCents(sum)}`。空列表时显示居中浅色文案"这个月还没有记账"。

- [ ] **步骤 4：类型检查并提交**

```bash
npx tsc --noEmit && git add src/components/ExpenseList.tsx src/components/ExpenseItem.tsx src/components/MonthHeader.tsx && git commit -m "feat: 按月列表、每日小计与月份切换"
```

---

## 任务 12：设置面板 `src/components/SettingsSheet.tsx`

**产出：** 备份导出/导入、数据体积、归类规则帮助页。

**接口：**
- 消费：`buildBackup`、`validateBackup`、`mergeExpenses`、`backupFileName`（backup.ts）；`estimateUsageBytes`、`loadSettings`、`saveSettings`（storage.ts）
- 产出：`<SettingsSheet open onClose onApply={({mode, expenses}) => Promise<boolean>} />`

- [ ] **步骤 1：实现导出**

```ts
const backup = buildBackup(expenses)
const blob = new Blob([JSON.stringify(backup, null, 2)], { type: 'application/json' })
const url = URL.createObjectURL(blob)
const a = document.createElement('a')
a.href = url
a.download = backupFileName(todayStr())
a.click()
URL.revokeObjectURL(url)
```
导出成功后写 `saveSettings({ ...settings, lastBackupAt: new Date().toISOString() })`。

- [ ] **步骤 2：实现导入**

`<input type="file" accept="application/json">` → 读文本 → `JSON.parse`（解析失败给友好提示）→ `validateBackup`。校验失败时**用 `Toast` 显示 `error` 并中止，不动现有数据**。校验通过后弹选择：「完全覆盖」（二次确认）/「合并进来」，把选中的 `expenses` 交给 `onApply`。

- [ ] **步骤 3：实现帮助区**

把 [产品设计文档.md](产品设计文档.md) 第 4.1 节的归类规则表渲染成一个可折叠列表，至少包含这几条最常用的：点外卖记餐饮›外卖、打车记交通›打车、宽带记通讯、快递费记生活服务、信用卡/花呗还款不记、请客吃饭记人情往来。

- [ ] **步骤 4：数据体积显示**

`estimateUsageBytes()` 超过 1MB 时显示一行提醒"数据已超过 1MB，建议导出备份"。

- [ ] **步骤 5：类型检查并提交**

```bash
npx tsc --noEmit && git add src/components/SettingsSheet.tsx && git commit -m "feat: 设置面板，导出/导入备份与归类规则帮助"
```

---

## 任务 13：组装 `src/App.tsx`

**产出：** 完整可用的应用。

**接口：**
- 消费：任务 7–12 的全部产出

- [ ] **步骤 1：实现**

状态：`monthKey`（初始 `monthKeyOf(todayStr())`）、`formOpen`、`editing: Expense | null`、`settingsOpen`、`toast`、`pendingDelete: Expense | null`（用于撤销）。

行为：
- 顶部 `MonthHeader`
- 中间 `ExpenseList`（数据来自 `byMonth.get(monthKey) ?? []`）
- 右下角悬浮「+」按钮，固定在 `bottom-6 right-4`，带 `safe-bottom`
- 点列表某条 → 打开表单编辑
- 删除：先 `removeExpense`，保存 `pendingDelete`，弹 `Toast`「已删除」+ 操作「撤销」，5 秒后自动 `setPendingDelete(null)`
- 保存失败时用 `Toast` 的 `error` 显示 `error` 文案
- 备份提醒：启动时 `loadSettings()`，若 `lastBackupAt` 为空或距今超过 30 天，显示 `Toast`「已经很久没备份了」+ 操作「立即备份」（打开设置面板）
- 表单和设置面板用底部弹出式遮罩层

- [ ] **步骤 2：类型检查 + 构建**

```bash
npx tsc --noEmit && npm run build
```
预期：均无错误。

- [ ] **步骤 3：提交**

```bash
git add src/App.tsx && git commit -m "feat: 组装主界面，完成记账闭环"
```

---

## 任务 14：手机打磨与端到端验收

**产出：** 在真机上验证过的可用版本。

- [ ] **步骤 1：跑全部测试**

```bash
npx vitest run
```
预期：全绿。

- [ ] **步骤 2：用打包版验收**

```bash
npm run build && npm run preview
```
⚠️ 必须用 `preview`，`dev` 模式下 PWA 离线功能不生效。

- [ ] **步骤 3：走完产品文档 8.1 节的 20 条手动验收清单**

逐条核对 [产品设计文档.md](产品设计文档.md) 第 8.1 节。**第 14 条（导出→导入→再导出，两个文件内容一致）必须做两遍。**

- [ ] **步骤 4：真机检查**

同一 WiFi 下用 `npm run preview -- --host` 给出的局域网地址在手机上打开，检查：无横向滚动条、底部按钮不被遮挡、输入金额时弹数字键盘、添加到主屏幕后无地址栏。

- [ ] **步骤 5：写 `README.md`**

内容：这个 App 是什么；怎么本地跑（`npm install` / `npm run dev` / `npm run build` / `npm run preview`）；怎么部署到 GitHub Pages；**怎么备份和恢复数据（最重要的一节）**；已知的坑（iPhone 双存储、微信浏览器、清除浏览器数据会丢账）。

- [ ] **步骤 6：提交并打标签**

```bash
git add -A && git commit -m "docs: 添加 README 与使用说明" && git tag v1.0.0
```

---

## 任务 15：部署到 GitHub Pages

**产出：** 一个手机随时能打开的网址。

> ⚠️ **这一步需要用户参与**：注册 GitHub 账号、在浏览器里完成登录授权。执行前先向用户说明需要做什么、大约几分钟。

- [ ] **步骤 1：确认用户已有 GitHub 账号并已登录**

若没有，给用户注册链接和步骤（邮箱注册，免费）。**不要替用户注册。**

- [ ] **步骤 2：创建远程仓库并推送**

```bash
gh auth status || gh auth login
gh repo create pingmian-jizhang --public --source=. --remote=origin --push
```

- [ ] **步骤 3：配置 GitHub Pages**

在仓库 Settings → Pages 里把 Source 设为 "GitHub Actions"，或用 `gh api` 配置。添加 `.github/workflows/deploy.yml`：构建时带 `VITE_BASE=/pingmian-jizhang/`，产物推到 Pages。

- [ ] **步骤 4：验证**

等 Actions 跑完，用手机打开 `https://<用户名>.github.io/pingmian-jizhang/`，确认能记账、能添加到主屏幕、飞行模式下能打开。

- [ ] **步骤 5：把网址告诉用户，并提醒先做一次备份**

---

## 自查结果

**规格覆盖检查**（对照产品设计文档 3.1 节功能清单）：

| 需求 | 对应任务 |
|---|---|
| F1 记一笔支出 | 任务 10、13 |
| F2 账目列表（按天分组 + 每日小计） | 任务 11 |
| F3 月份切换 | 任务 11、13 |
| F4 本月总支出 | 任务 7、11 |
| F5 编辑账目 | 任务 10、13 |
| F6 删除 + 5 秒撤销 | 任务 7、13 |
| F7 导出备份 | 任务 6、12 |
| F8 导入备份（校验 + 覆盖/合并） | 任务 6、12 |
| F9 备份提醒（30 天） | 任务 13 |
| F10 归类规则帮助页 | 任务 12 |
| F11 添加到手机主屏幕 | 任务 1、14、15 |

**8 个已决定的产品细节**：① 分类固定（无分类管理任务，正确）② 二级必选（任务 9）③ 删除+撤销（任务 13）④ 导入时询问（任务 12）⑤ 可补记日期（任务 10）⑥ 每日小计（任务 11）⑦ 30 天备份提醒（任务 13）⑧ 纯数字键盘（任务 10，`inputMode="decimal"`）。

**类型一致性**：`NewExpense`（任务 7）在任务 10 的表单提交和任务 13 的调用处保持一致；`ValidationResult` 的判别字段 `ok` 在任务 6 和任务 12 中一致；`commit` 返回 `boolean` 用于任务 10 的"保存失败不关闭表单"。

**明确不在本计划内**（产品文档 3.2 节已排除）：收入、图表、预算、Excel 导出、账户管理、云端同步、自定义分类、搜索、计算器。
