# 平面记账 — 产品设计文档

> 版本：v1.0
> 日期：2026-09-17
> 状态：✅ 产品与技术选型均已确认，可以开始开发
>
> 配套文档：[CLAUDE.md](CLAUDE.md) —— 工作准则、决策记录、数据模型约定

---

## 一、这是什么

**平面记账**是一个给自己用的极简记账工具。核心只有一件事：

> 花完钱的那一刻，用手机花 5 秒钟把它记下来。

它不追求功能大而全，追求的是**你能坚持记下去**。所以第一版刻意做得很窄：只记支出、分类固定、数据存自己手机上、没有登录、没有服务器。

### 1.1 目标用户

- 想搞清楚"钱到底花在哪了"的普通人
- 不想注册账号、不想把消费记录上传到别人服务器的人
- 用过记账 App 但因为"太麻烦"而放弃过的人

### 1.2 使用场景

| 场景 | 用户动作 | 期望结果 |
|---|---|---|
| 刚吃完午饭付了 28 元 | 掏出手机 → 点「+」→ 输 28 → 选「餐饮 › 午餐」→ 保存 | 全过程不超过 10 秒 |
| 晚上想看今天花了多少 | 打开 App | 一眼看到今天的每笔账和当日小计 |
| 月底想知道这个月花了多少 | 打开 App | 顶部直接显示本月总支出 |
| 发现三天前有笔账记错了 | 找到那条 → 点开 → 改金额 → 保存 | 金额和总额同步更新 |
| 换了新手机 | 旧手机导出备份文件 → 新手机导入 | 账目一条不少 |

---

## 二、核心概念

用大白话解释这个 App 里的几个词：

- **一笔账（记录）**：你花出去的一笔钱。包含「多少钱、花在哪类、什么时候、备注」。
- **一级大类**：最粗的分类，比如「餐饮」「交通」「购物」。共 12 个。
- **二级小类**：大类下面更细的分类，比如「餐饮」下面的「早餐」「外卖」「买菜做饭」。共约 97 个。
- **本地存储**：你的账目存在自己手机/电脑的浏览器里，**没有上传到任何服务器**。
- **备份文件**：一个 `.json` 文件，把你所有账目打包导出。换手机、或者浏览器数据被清掉时，靠它恢复。

---

## 三、功能清单

### 3.1 第一版要做（v1）

| 编号 | 功能 | 说明 | 优先级 |
|---|---|---|---|
| F1 | 记一笔支出 | 金额（元，精确到分）+ 两级分类 + 可选备注 + 日期（默认今天，可改成前几天） | 必须 |
| F2 | 账目列表 | 按天分组，最新在上；每天标题显示当日小计 | 必须 |
| F3 | 月份切换 | 可以往前翻看往月的账 | 必须 |
| F4 | 本月总支出 | 顶部固定显示当前所选月份的总支出 | 必须 |
| F5 | 编辑账目 | 点开某条记录改金额/分类/备注/日期 | 必须 |
| F6 | 删除账目 | 删除后 5 秒内顶部出现「撤销」按钮，可恢复 | 必须 |
| F7 | 导出备份 | 导出一个 JSON 文件，含全部账目 + 分类表 | 必须 |
| F8 | 导入备份 | 导入前显示文件条数，让用户选「完全覆盖」或「合并」；文件损坏要报错且不影响现有数据 | 必须 |
| F9 | 备份提醒 | 超过 30 天没备份，打开 App 时顶部提示一次，点一下就导出 | 必须 |
| F10 | 归类规则帮助页 | 把「外卖算餐饮不算购物」这类灰色地带的判断规则写进 App | 应该 |
| F11 | 添加到手机主屏幕 | 手机上像 App 一样有图标、全屏、可离线使用 | 应该 |

### 3.2 第一版明确不做（留到 v2 再说）

| 功能 | 为什么先不做 |
|---|---|
| 收入记录 | 用户已确认 v1 只记支出。数据结构预留了扩展空间，以后加不难 |
| 统计图表（饼图、趋势图） | 会增加开发量；先用「本月总支出 + 每日小计」满足基本需求 |
| 月度预算与超支提醒 | 需要额外的设置页和提醒逻辑；等记账习惯养成后再加更合适 |
| 导出 Excel / CSV | 备份用 JSON 就够了。Excel 导出属于「在电脑上再分析」的需求，v2 再说 |
| 多账户管理（微信/支付宝/银行卡） | 复杂度高，容易记乱 |
| 云端同步 / 账号登录 | 用户已确认只存本地 |
| 自定义分类 | v1 分类固定。用一两个月后如果确实缺某类，再加编辑功能，那时才知道具体缺什么 |
| 搜索功能 | 记录少的时候翻列表就够；等记录超过 500 条再考虑 |
| 记账计算器（输入 `12.5*3` 自动算） | 会让金额字段从「一个数」变成「一个算式」，增加算错和导出文件难读的风险 |

---

## 四、两级分类体系

共 **12 个一级大类、约 97 个二级小类**。

括号里是**稳定编号**（程序内部用的 ID，永不改变）。你看到的中文名以后可以改，改了之后所有历史记录会自动跟着显示新名字。

### 1. 餐饮 `food`（11 个）

| 二级小类 | 编号 |
|---|---|
| 早餐 | `food.breakfast` |
| 午餐 | `food.lunch` |
| 晚餐 | `food.dinner` |
| 夜宵 | `food.late_night` |
| 外卖 | `food.takeout` |
| 买菜做饭 | `food.groceries` |
| 零食饮料 | `food.snacks` |
| 水果 | `food.fruit` |
| 咖啡奶茶 | `food.coffee_tea` |
| 烟酒 | `food.tobacco_alcohol` |
| 聚餐下馆子 | `food.dining_out` |

### 2. 交通 `transport`（11 个）

| 二级小类 | 编号 |
|---|---|
| 公交地铁 | `transport.public` |
| 打车 | `transport.taxi` |
| 共享单车 / 电动车 | `transport.bike` |
| 加油 | `transport.fuel` |
| 停车费 | `transport.parking` |
| 过路费 | `transport.toll` |
| 火车 / 高铁 | `transport.train` |
| 飞机 | `transport.flight` |
| 长途汽车 | `transport.coach` |
| 车辆保养维修 | `transport.maintenance` |
| 违章罚款 | `transport.fine` |

### 3. 购物 `shopping`（9 个）

| 二级小类 | 编号 |
|---|---|
| 日用百货 | `shopping.daily` |
| 服饰鞋包 | `shopping.clothing` |
| 化妆护肤 | `shopping.beauty` |
| 数码电器 | `shopping.digital` |
| 家居用品 | `shopping.home` |
| 母婴用品 | `shopping.baby` |
| 运动户外 | `shopping.sports` |
| 珠宝配饰 | `shopping.jewelry` |
| 其他购物 | `shopping.other` |

### 4. 居住 `housing`（10 个）

| 二级小类 | 编号 |
|---|---|
| 房租 | `housing.rent` |
| 房贷 | `housing.mortgage` |
| 物业费 | `housing.property_fee` |
| 水费 | `housing.water` |
| 电费 | `housing.electricity` |
| 燃气费 | `housing.gas` |
| 取暖费 | `housing.heating` |
| 房屋维修 | `housing.repair` |
| 装修 | `housing.renovation` |
| 搬家 / 中介费 | `housing.moving_agency` |

### 5. 通讯 `comm`（5 个）

| 二级小类 | 编号 |
|---|---|
| 手机话费 | `comm.phone_bill` |
| 宽带 / 网费 | `comm.broadband` |
| 流量包 | `comm.data` |
| 有线电视 | `comm.cable_tv` |
| 手机维修 | `comm.repair` |

### 6. 医疗健康 `medical`（9 个）

| 二级小类 | 编号 |
|---|---|
| 门诊挂号 | `medical.clinic` |
| 药品 | `medical.medicine` |
| 住院 | `medical.hospital` |
| 体检 | `medical.checkup` |
| 口腔牙科 | `medical.dental` |
| 眼科 / 配镜 | `medical.optical` |
| 保健品 | `medical.supplement` |
| 中医理疗 | `medical.tcm` |
| 疫苗 | `medical.vaccine` |

### 7. 教育 `education`（9 个）

| 二级小类 | 编号 |
|---|---|
| 学费 | `education.tuition` |
| 培训课程 | `education.training` |
| 兴趣班 | `education.hobby_class` |
| 家教 | `education.tutor` |
| 书籍教材 | `education.books` |
| 文具用品 | `education.stationery` |
| 考试报名费 | `education.exam_fee` |
| 网课 / 知识付费 | `education.online_course` |
| 学校杂费 | `education.school_misc` |

### 8. 娱乐 `ent`（8 个）

| 二级小类 | 编号 |
|---|---|
| 电影演出 | `ent.movie_show` |
| 旅游度假 | `ent.travel` |
| 景点门票 | `ent.tickets` |
| 运动健身 | `ent.fitness` |
| 游戏充值 | `ent.games` |
| 会员订阅 | `ent.subscription` |
| 聚会娱乐 | `ent.party` |
| 兴趣爱好 | `ent.hobby` |

### 9. 人情往来 `social`（7 个）

| 二级小类 | 编号 |
|---|---|
| 红包 | `social.red_packet` |
| 份子钱 / 随礼 | `social.wedding_gift` |
| 礼品礼物 | `social.gift` |
| 孝敬长辈 | `social.elders` |
| 请客招待 | `social.treating` |
| 探病慰问 | `social.visit` |
| 慈善捐赠 | `social.donation` |

### 10. 生活服务 `services`（8 个）

| 二级小类 | 编号 |
|---|---|
| 理发美容 | `services.hair_beauty` |
| 洗衣干洗 | `services.laundry` |
| 家政保洁 | `services.cleaning` |
| 快递邮寄 | `services.courier` |
| 家电维修 | `services.appliance_repair` |
| 打印复印 | `services.printing` |
| 照相 / 证件照 | `services.photo` |
| 办事手续费 | `services.admin_fee` |

### 11. 宠物 `pets`（7 个）

| 二级小类 | 编号 |
|---|---|
| 宠物口粮 | `pets.food` |
| 宠物用品 | `pets.supplies` |
| 宠物医疗 | `pets.medical` |
| 宠物美容 | `pets.grooming` |
| 宠物寄养 | `pets.boarding` |
| 宠物药品 | `pets.medicine` |
| 宠物训练 | `pets.training` |

### 12. 其他 `other`（5 个）

| 二级小类 | 编号 |
|---|---|
| 办公 / 工作支出 | `other.work` |
| 手续费与利息 | `other.fees` |
| 意外损失 | `other.loss` |
| 待分类 | `other.unsorted` |
| 其他杂项 | `other.misc` |

---

### 4.1 归类规则（灰色地带怎么判）

中文记账最容易纠结的就是"这笔到底算哪类"。下面这些规则会写进 App 的「帮助」页，也写在这里作为设计依据：

| 情况 | 记到哪 | 为什么 |
|---|---|---|
| 点外卖（不论哪一餐） | 餐饮 › 外卖 | "外卖"是一种获取方式，不要再记到"午餐" |
| 堂食或打包带走 | 餐饮 › 早餐 / 午餐 / 晚餐 | |
| 和朋友 AA 吃饭 | 餐饮 › 聚餐下馆子 | |
| **我买单请别人吃饭** | 人情往来 › 请客招待 | 这是"人情支出"，和"自己吃饭"是两件事 |
| 超市买菜买肉 | 餐饮 › 买菜做饭 | |
| 超市买纸巾洗衣液 | 购物 › 日用百货 | 一单里既有菜又有日用品：按金额大的那部分记，或拆成两笔 |
| 网购 | **不单列** | 按买的东西归类。网购是"渠道"不是"类别" |
| 打车 / 网约车 / 出租车 | 交通 › 打车 | 绝不能记到"购物" |
| 单车月卡、电动车充电 | 交通 › 共享单车 / 电动车 | |
| 手机话费、流量 | 通讯 › 手机话费 | |
| 宽带、网费 | 通讯 › 宽带 / 网费 | 不要记到"居住"，否则"居住"会虚高 |
| 房租、水电燃气、物业、取暖 | 居住 | 这些是"住"的固定成本 |
| 快递费、寄件费 | 生活服务 › 快递邮寄 | 不要记到"购物" |
| **信用卡还款、花呗还款、自己账户间转账** | **不记** | 记了会和消费重复计算，总额翻倍。只有"手续费 / 利息"才记 `other.fees` |
| 借给别人的钱 | 不记（借钱不是消费） | 如果确定收不回来了，记 `other.loss` |
| 买礼物送人 | 人情往来 › 礼品礼物 | 给自己买衣服才是"购物 › 服饰鞋包" |
| 健身房年卡、场地费 | 娱乐 › 运动健身 | 买运动装备 → 购物 › 运动户外 |
| 给父母的过节钱 | 人情往来 › 孝敬长辈 | |
| 朋友结婚随份子 | 人情往来 › 份子钱 / 随礼 | |
| 孩子的学费 / 兴趣班 | 教育 | v1 不区分"自己用"和"孩子用"，保持简单 |
| 买书、买文具 | 教育 › 书籍教材 / 文具用品 | |
| 开车违章罚款 | 交通 › 违章罚款 | 其他类型的罚款 → 其他 › 手续费与利息 |
| 医院开的药 | 医疗健康 › 药品 | 挂号费单独记 → 门诊挂号 |
| 猫粮狗粮 | 宠物 › 宠物口粮 | |

---

## 五、界面与交互

### 5.1 只有两个界面

**主界面**

```
┌─────────────────────────────┐
│  ‹   2026 年 9 月   ›        │  ← 月份切换
│                             │
│  本月支出                    │
│  ¥ 3,280.00                 │  ← 大号字，一眼看到
│                             │
├─────────────────────────────┤
│  今天               ¥96.50  │  ← 日期 + 当日小计
│  🍜 餐饮 · 午餐      ¥28.00 │
│  🚇 交通 · 地铁       ¥5.00 │
│  🛒 购物 · 日用品    ¥63.50 │
├─────────────────────────────┤
│  昨天               ¥42.00  │
│  ☕ 餐饮 · 咖啡奶茶  ¥18.00 │
│  🍱 餐饮 · 外卖      ¥24.00 │
│                             │
│                        ( + )│  ← 右下角悬浮按钮
└─────────────────────────────┘
```

**记账界面**（点右下角「+」后从底部弹出）

```
┌─────────────────────────────┐
│  记一笔                  ✕  │
│                             │
│  ¥  [        28.00       ]  │  ← 自动弹数字键盘
│                             │
│  分类                       │
│  ┌────┬────┬────┬────┐      │
│  │餐饮│交通│购物│居住│  ... │  ← 先点大类
│  └────┴────┴────┴────┘      │
│  ┌────┬────┬────┬────┐      │
│  │早餐│午餐│晚餐│外卖│  ... │  ← 再点小类，自动收起
│  └────┴────┴────┴────┘      │
│                             │
│  备注（可不填）              │
│  [ 公司楼下              ]  │
│                             │
│  日期    [ 今天 ▾ ]         │
│                             │
│  [        保 存        ]    │
└─────────────────────────────┘
```

### 5.2 关键交互决定

| 决定 | 做法 | 理由 |
|---|---|---|
| 记一笔要几步 | 点「+」→ 输金额 → 点大类 → 点小类 → 保存（4 次点击） | 最少步骤完成最完整的记录 |
| 二级小类是否必选 | **必选** | 多花 1 秒，换来"外卖一年花了多少"这种有价值的明细 |
| 金额输入 | 弹数字键盘，不接受文字 | 手机上最快，且从源头杜绝非法输入 |
| 删除 | 直接删，但顶部给 5 秒「撤销」 | 界面简单，误删几乎立刻能救回来 |
| 日期 | 默认今天，可点开改成前几天 | 漏记一天第二天补上是很常见的事 |
| 图标 | 用"首字方块"（把"餐""交""购"做成圆角色块） | 零成本、风格统一、不用引入图标库 |

---

## 六、技术方案选型（✅ 已于 2026-09-17 决定）

> **你已做出的选择：主技术栈选 A（React + TypeScript + Vite + Tailwind），本地存储选 A（localStorage），部署选 A（GitHub Pages），8 个产品细节全部采纳推荐项。**
>
> 下面保留了当初对比过的所有方案和被否决的理由，方便你以后回溯"当时为什么没选那个"。

### 6.1 主方案选择：用什么技术来写 ✅ 已选 A

| | **方案 A：现代网页应用**（React + TypeScript + Vite + Tailwind）✅ **已采用** | **方案 B：单个 HTML 文件**（纯 HTML + JavaScript）❌ 已否决 | **方案 C：Vue / Svelte 网页应用** ❌ 已否决 |
|---|---|---|---|
| 需要装开发工具吗 | 需要（`npm install` 一次，我来做） | 完全不需要 | 需要 |
| 代码规模 | 约 1500–2000 行，拆成 20 来个文件 | 约 800–1200 行挤在 1 个文件里 | 约 1200–1600 行 |
| **写错时谁先发现** | **TypeScript 在你打开浏览器之前就红字报错** | 只有真正用到时才会发现 | 同 A，但提示生态稍弱 |
| 手机桌面图标（PWA） | 插件自动生成，最省事 | 要手写配置文件，容易配错 | 同 A |
| 界面好看程度 | Tailwind 让手机端样式很好调 | 要手写 CSS，调起来慢 | 同 A |
| 以后加图表 / 预算 | 直接加 | 文件会越写越乱 | 直接加 |
| 长期维护 | 低风险，资料最多 | 超过 1500 行后很难改 | 低风险 |

**你选择 A 的理由**：
1. 要"写错能提前报错"这层保险 —— 这个 App 你会用很久，金额、日期这类地方出错是肉眼看不出来的。
2. 以后加图表、预算等功能很容易。

**方案 B 的实话**：它其实很香 —— 一个文件，双击就能开，永远不会坏。但有两个硬伤：没有类型检查（金额、日期这类地方最容易埋 bug），以及手机"添加到主屏幕 + 离线可用"还是需要架服务器，逃不掉。

**方案 C 的实话**：Vue 和 Svelte 都很好，Vue 的中文文档甚至是三者里最好的。但 Svelte 5 的 API 较新、示例少；Vue 在"AI 生成代码的准确率"上略逊 React。差异不大，纯粹因为是我来写，所以选我最熟练的。

**方案 B 的实话**：它其实很香 —— 一个文件，双击就能开，永远不会坏。但它有两个硬伤：没有类型检查（金额、日期这类地方最容易埋 bug），以及手机"添加到主屏幕 + 离线可用"还是需要架服务器，逃不掉。

**方案 C 的实话**：Vue 和 Svelte 都很好，Vue 的中文文档甚至是三者里最好的。但 Svelte 5 的 API 较新、示例少；Vue 在"AI 生成代码的准确率"上略逊 React。差异不大，纯粹因为是我来写，所以选我最熟练的。

### 6.2 子方案：数据存在浏览器的哪里 ✅ 已选 A

| | **方案 A：localStorage** ✅ **已采用** | **方案 B：IndexedDB** ❌ 已否决 |
|---|---|---|
| 通俗比喻 | 一张便签纸，读写瞬间完成 | 一个真正的小型数据库，功能强但每次操作都要"等一下" |
| 代码复杂度 | 低，几乎不会出错 | 高，到处要处理"加载中"、升级失败、被其他标签页占用 |
| 容量 | 约 5MB（够记 8–10 年） | 几乎无限 |
| 按月份筛选的速度 | 几万条以内都是零点几毫秒 | 有索引，更快（但这里用不上） |
| 导出备份 | 一个 JSON 字符串直接写文件 | 要游标分批读 |

**你选择 A 的理由**：代码越简单，未来的数据越安全；容量 8–10 年够用。

**为什么 IndexedDB 被否决**：按每天记 8 笔算，1 年约 0.5MB，打满 5MB 要 8–10 年。而唯一的查询（按月份筛选）就是在内存数组里过滤一遍，几万条以内耗时可以忽略 —— IndexedDB 的索引优势在这里毫无价值，却要付出"到处处理异步状态"的复杂度代价。

**兜底措施**：存储超过 1MB（约 5000 条，够用 2 年）时在设置页显示提醒；空间不足时明确弹窗告知，绝不静默失败。

**给未来留门**：所有存储函数一律写成"异步"形式，将来真要换 IndexedDB 时只改一个文件，其他代码一行不动。**切换的触发条件（写死在代码注释里）：数据超 1.5MB，或记录超 10000 条，或要加多设备同步 —— 满足任一条就换。**

### 6.3 部署方式（让手机能打开这个网址）✅ 已选 A

| | **方案 A：GitHub Pages** ✅ **已采用** | **方案 B：Vercel / Netlify** ❌ 已否决 | **方案 C：只在自己电脑上跑** ❌ 已否决 |
|---|---|---|---|
| 花钱 | 免费 | 免费 | 免费 |
| 需要注册账号吗 | 需要 GitHub 账号 | 需要注册 | 不需要 |
| 上手难度 | 中（要学 Git 推送） | 低（连 GitHub 一键部署） | 最低 |
| 国内访问速度 | 一般（偶尔慢） | 一般 | 最快（局域网） |
| 手机随时能用 | ✅ | ✅ | ❌ 只有在家连同一个 WiFi 才行 |

**你选择 A 的理由**：免费、手机随时能开（不限在家）、顺便把代码也托管了 —— 相当于给代码也做了备份。**你会失去的**：需要注册一个 GitHub 账号（邮箱就能注册，免费，3 分钟）。Git 推送的操作由我来做，但注册和登录需要你自己完成。

> ⚠️ 注意：无论选哪个，都必须走 HTTPS。用 `file://` 双击打开 HTML 的方式，Service Worker（离线功能）根本不工作。

### 6.4 8 个产品细节 ✅ 全部采纳推荐项

下表第 3 列列出当初对比的两个选项，第 4 列是**最终决定**。你选择全部采纳推荐项，理由是这些都是"用最少复杂度换取最大实用性"的那个。

| # | 问题 | 选项 | ✅ 最终决定 |
|---|---|---|---|
| ① | **分类能不能自己改？** | A. 完全固定（12 大类 97 小类，不能增删改）<br>B. 可以自己增删改（要多做分类管理页，约多 300 行代码；且"删掉一个正在用的分类"会导致历史记录显示异常，需要额外保护逻辑） | **A**。用一两个月后如果确实缺某类，再加也不迟，那时你会知道具体缺什么 |
| ② | **每笔都必须选到二级小类吗？** | A. 必选<br>B. 可以不选，直接落在大类上（记账快 1 秒，但半年后"餐饮"这一大块看不出细节） | **A** |
| ③ | **删除是直接删还是进回收站？** | A. 直接删 + 5 秒撤销<br>B. 进回收站保留 30 天（多做一个页面 + 清理逻辑 + 占空间） | **A** |
| ④ | **导入备份时怎么办？** | A. 每次都问你，显示文件条数后给"完全覆盖"和"合并进来"两个按钮<br>B. 默认覆盖（换手机最省事，但漏导过一次就会把新账抹掉） | **A**，且"覆盖"要二次确认 |
| ⑤ | **只能记今天，还是能补记？** | A. 可以改日期（默认今天）<br>B. 只能记今天（漏记就永远补不回来） | **A**，成本几乎为零 |
| ⑥ | **列表里要每天的小计吗？** | A. 显示（每天标题右边写 ¥86.50）<br>B. 不显示，只有月总计 | **A**，约 10 行代码，v1 里性价比最高的小功能 |
| ⑦ | **什么时候提醒备份？** | A. 超 30 天没备份就提示一次<br>B. 完全手动（很可能半年都不备份一次）<br>C. 每月 1 号固定提醒 | **A**。备份是你数据唯一的保险 |
| ⑧ | **金额输入要不要带计算器？** | A. 纯数字键盘<br>B. 能算 `12.5*3`（AA 分账方便，但会引入"算错了看不出来"的风险，且导出文件可读性下降） | **A**，v2 再考虑 |

---

## 七、数据安全与备份策略

数据只存在你自己的设备上 —— **这既是最安全的（没人在偷看），也是最脆弱的（没人帮你存着）**。所以备份机制是 v1 的必做项，不是可选项。

### 7.1 三层保护

1. **请求永久存储**：App 启动时向浏览器申请"永久保存"权限，降低被系统清理空间时删掉的概率。
2. **30 天备份提醒**：超过 30 天没导出，打开 App 时顶部显示提示条，点一下就导出。不打扰，但不会让你忘。
3. **导出 / 导入闭环**：导出的 JSON 文件里**同时包含账目和完整分类表**，所以文件是"自解释"的 —— 用记事本打开就能看懂每一笔是什么。

### 7.2 ⚠️ 三个必须知道的坑

1. **iPhone 上，「Safari 里的网页」和「添加到主屏幕的 App」存储是分开的，两边数据不互通。** 请从一开始就固定用其中一种方式记账，换方式之前先导出备份。
2. **微信内置浏览器不能"添加到主屏幕"，且清理微信缓存可能清掉数据。** 请用 Safari / Chrome / 系统自带浏览器打开。
3. **清除浏览器数据 = 清空记账数据。** 导出备份是唯一的救命手段。

### 7.3 备份文件长什么样

文件名：`平面记账-备份-2026-09-17.json`

```json
{
  "app": "pingmian-jizhang",
  "schemaVersion": 1,
  "exportedAt": "2026-09-17T08:30:00.000Z",
  "categories": [
    { "id": "food", "name": "餐饮", "parentId": null, "sortOrder": 10 },
    { "id": "food.takeout", "name": "外卖", "parentId": "food", "sortOrder": 50 }
  ],
  "expenses": [
    {
      "id": "3f2a...",
      "amountCents": 2350,
      "categoryId": "food",
      "subcategoryId": "food.takeout",
      "note": "午饭 麻辣烫",
      "date": "2026-09-17",
      "createdAt": "2026-09-17T04:12:33.000Z",
      "updatedAt": "2026-09-17T04:12:33.000Z"
    }
  ]
}
```

**导入时必须校验**（这是唯一的救命通道，不能马虎）：检查是不是本 App 的文件、版本号是否兼容、账目是不是一个列表、每笔金额是不是合法整数、日期格式对不对。**任何一条不合格就整体拒绝并说明第几条出错，绝不半途写入。**

---

## 八、验收标准

### 8.1 你看得懂的手动验收清单

**准备**：先跑 `npm run build` 然后 `npm run preview`，用**打包版**测试。⚠️ 普通开发模式下离线功能不会生效，这是最容易误判"功能坏了"的地方。

| # | 做什么 | 期望看到什么 |
|---|---|---|
| 1 | 记一笔 `0.01` | 列表显示 `¥0.01`，月总额 `¥0.01` |
| 2 | 记一笔 `12.34`，再记一笔 `0.02` | 月总额是 `¥12.37`（**不是** 12.360000000000001） |
| 3 | 金额输入框里试：留空、输「abc」、输「0」、输「-5」、输「1.234」 | 五种都点不动「保存」或明确提示错误，不能存进去 |
| 4 | 把手机时间改到 23:50 记一笔 | 日期还是"今天"，不会跳到明天 |
| 5 | 编辑刚记的一笔，金额改成 100 | 列表和月总额同步更新 |
| 6 | 删除一笔 | 记录消失，总额减少，顶部出现「撤销」，5 秒内点它能回来 |
| 7 | 切换到上个月 / 下个月 | 列表和总额只显示所选月份，切回来数据不丢 |
| 8 | 记两笔都是今天的账 | 后记的那笔排在列表上面 |
| 9 | **刷新页面** | 数据全在 |
| 10 | **完全关掉浏览器再打开** | 数据全在 |
| 11 | 备注里输 200 个字 | 布局不破，自动换行或截断 |
| 12 | 导出备份 | 下载一个 JSON；用记事本打开能看到中文分类名 |
| 13 | 换一个浏览器打开同一个网址（或点"清空数据"） | 变成空的 |
| 14 | **导入刚才的备份** | 条数、金额、日期、分类和导出前**完全一致** |
| 15 | 把备份文件里的金额手动改成一个非法值再导入 | 明确报错并指出第几条有问题，**现有数据不受影响** |
| 16 | 用手机 Safari / Chrome 打开 | 能正常记账，没有横向滚动条 |
| 17 | 「添加到主屏幕」 | 桌面出现图标；从图标打开时没有地址栏，像原生 App |
| 18 | 从主屏幕图标打开后，**开飞行模式**再记账 | 完全正常，能存能看 |
| 19 | 在 iPhone SE 和 iPhone Pro Max 两种尺寸下看 | 都不挤、不裁切 |
| 20 | iOS：先在 Safari 里记一笔，再添加到主屏幕打开 | 确认哪一边有数据，然后**固定只在一边记** |

> **第 14 条是整个验收里最重要的一条** —— 它是数据丢失时唯一的补救手段，必须反复验证两次（导出 → 导入 → 再导出，两个文件内容应完全一致）。

### 8.2 自动测试（只测"肉眼看不出来的错误"）

建议加 **Vitest**，只测纯逻辑函数，**不测界面**。约 20 个测试，覆盖：

- **金额换算**：`'12.34'→1234`、`'0.01'→1`、全角 `'１２．３４'→1234`、`'¥12.3'→1230`；非法输入（空、`abc`、`0`、`-5`、`1.234`、`1e5`）全部返回失败
- **日期**：跨月、跨年（12月31日→1月1日）、闰年 2 月 29 日
- **分类表完整性**：97 条分类里 ID 有没有重复、小类指向的大类是否存在、大类名字有没有空的
- **备份校验**：缺字段、金额是小数、账目不是列表 —— 都应被拒绝

**不装的东西**：Playwright / Cypress（浏览器自动化点击测试）。对这个体量，写自动化脚本的成本远高于手动点几分钟，而且维护成本会一直存在。

---

## 九、后续版本规划（现在不做，只是让你知道路怎么走）

| 版本 | 可能的内容 | 前提 |
|---|---|---|
| v1.1 | 统计图表（分类饼图、每日柱状图、月度趋势） | v1 用顺了之后 |
| v1.2 | 月度预算 + 超支提醒 | 记账习惯养成后 |
| v1.3 | 导出 Excel / CSV | 有在电脑上再分析的需求时 |
| v2.0 | 自定义分类（增删改） | 用了一两个月，明确知道缺哪类时 |
| v2.x | 收入记录、账户管理 | 需求明确时 |
| v3.0 | 云端同步、多设备 | 只有一台设备不够用时 |

---

## 十、术语对照表（给非程序员的）

| 你会听到的词 | 大白话解释 |
|---|---|
| **TypeScript** | 一种"会提前报错"的 JavaScript。写错了在你打开网页之前就会红字提示 |
| **React** | 一个做网页界面的工具库，全世界用得最多 |
| **Vite** | 把源代码"打包"成浏览器能读的文件的工具，同时提供改完立刻刷新的开发体验 |
| **Tailwind** | 一套现成的样式积木。写 `text-lg` 就是"大号字"，不用自己写 CSS |
| **PWA** | 让网页能"添加到手机主屏幕、有图标、能离线用"的技术 |
| **Service Worker** | PWA 离线能力的底层机制。**必须 HTTPS 才能工作** |
| **localStorage** | 浏览器里的一张小仓库，存你的账目 |
| **JSON** | 一种文本格式，用来存数据。备份文件就是这个格式，记事本可读 |
| **npm / node_modules** | JavaScript 的"应用商店"和装下来的零件柜。你不需要碰 |
| **构建 / 打包** | 把源码加工成浏览器能直接跑的文件。一条命令的事 |
| **HTTPS** | 网址开头的 `https://`，加密连接。PWA 和离线功能的硬性要求 |
| **部署** | 把做好的网页放到网上，让你的手机能通过网址打开 |
